spring.application.name=demo
spring.security.user.name=user
spring.security.user.password=password

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/demoapp
spring.datasource.username=postgres
spring.datasource.password=BetaG1069982
spring.datasource.driver-class-name=org.postgresql.Driver
# Property to control the schema of the DB
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Disable Open Session in View to avoid DB access during view rendering
spring.jpa.open-in-view=false